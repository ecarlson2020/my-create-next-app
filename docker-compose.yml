services:
  mysql:
    # Match prod exactly: prod runs MariaDB 11.8.3 (see db/migrations dump
    # header). Using MySQL here breaks on MariaDB-only collations such as
    # utf8mb4_uca1400_ai_ci in the prod dump.
    image: mariadb:11.8.3
    container_name: mysql_dev
    # `no` rather than `unless-stopped`: a failed init script (e.g. a broken
    # migration) makes the entrypoint exit non-zero. With a restart policy,
    # the container comes back up and skips init because /var/lib/mysql is
    # already populated — the container ends up "healthy" with a half-
    # migrated schema. `no` lets the failure stay visible. Tradeoff: a real
    # crash during dev won't auto-recover; just run `npm run db` again.
    restart: "no"
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: evroca_db_dev
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_password
      TZ: America/Denver
    ports:
      - "3306:3306"
    volumes:
      # Persist MySQL data
      - mysql_data:/var/lib/mysql
      # Mount initialization scripts
      - ./db/init:/docker-entrypoint-initdb.d
      # Mount migrations for manual execution
      - ./db/migrations:/migrations
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app_network

  # Optional: phpMyAdmin for database management
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: phpmyadmin_dev
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: root_password
      UPLOAD_LIMIT: 100M
    ports:
      - "8080:80"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - app_network

volumes:
  mysql_data:
    driver: local

networks:
  app_network:
    driver: bridge
