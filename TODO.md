# To Do

- ~/out on raspberry pi is error prone. standardize across repos
  - [x] update every repo
- new versions of code are getting cached
- standarize `npm i` running when `npm run prod` runs or not across repos
